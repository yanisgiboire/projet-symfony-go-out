{% extends 'base.html.twig' %}

{% block title %}Annuler une sortie{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <a href="{{ path('app_go_out_index') }}" class="btn btn-secondary py-1 my-3" style="width: fit-content;"><i class="fa-solid fa-arrow-left"></i> Retour à la liste</a>
            <div class="col-md-12 card custom-card">
                <div class="card-header justify-content-between align-items-center">
                    <h1 class="text-center">Sortie : {{ go_out.name }}</h1>
                </div>
                <div class="card-body">
                    {{ include('go_out/_form_cancel.html.twig', {'button_label': 'Annuler la sortie'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
