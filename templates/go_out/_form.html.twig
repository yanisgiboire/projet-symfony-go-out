{{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    <button class="btn btn-primary w-100 my-1">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}