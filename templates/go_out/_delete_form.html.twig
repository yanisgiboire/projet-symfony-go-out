<form method="post" action="{{ path('app_go_out_delete', {'id': go_out.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ go_out.id) }}">
    <button class="btn">Delete</button>
</form>
