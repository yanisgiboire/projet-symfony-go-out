{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-9">
            <div class="goCard">
                <div class="d-flex justify-content-between align-items-center">
                    <h1 class="h2 mb-4 text-white text-center">Editer mon profil </h1>
                    <a class=" btn  btn-primary text-white" href="{{ path('app_user_password_edit', {'id': user.id}) }}">Modifier mon mot de passe</a>
                </div>
                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

            </div>
        </div>
    </div>
</div>
{% endblock %}
